# Copy this file to .env in the project root, then fill in values — or just run
# setup.ps1 / deploy.sh, which generate this file with random secrets automatically
# on first run (nothing below is required if you use those scripts).
# docker-compose reads .env automatically.

# ── OpenRouter (OPTIONAL) ──────────────────────────────────────────
# The AI Agent normally reuses whichever LLM provider you already configured in
# the app's Settings tab (Anthropic / OpenAI-compatible / Ollama / OpenRouter) —
# there is nothing to set up here for that. Only fill these in if you want to give
# every user of this deployment a shared OpenRouter key so they don't need their own.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=nousresearch/hermes-3-llama-3-8b

# ── Database passwords ────────────────────────────────────────────
POSTGRES_PASSWORD=changeme_in_production
AGENT_DB_PASSWORD=agent_changeme_in_production

# ── Backend API authentication (required) ─────────────────────────
# The backend refuses to start without this set. Generate one with:
#   openssl rand -hex 32
# This is wired through automatically at build time (see docker-compose.yml's
# frontend build arg) — the AI Agent authenticates with no manual step, nothing
# to paste into the UI.
BACKEND_API_TOKEN=

# Set to "true" ONLY for local-only testing with no authentication at all.
# Never set this to true on anything reachable outside your own machine.
ALLOW_UNAUTHENTICATED=false

# ── Frontend port ─────────────────────────────────────────────────
PORT=8080
